import * as service from '../services/projectionhomessService.js';

export function getProjectionhomess(req, res) {
    service.getProjectionhomess(req, res);
}

export function findByyear(req, res) {
    service.findByyear(req, res);
}

